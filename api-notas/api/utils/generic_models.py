# from enum import Enum
# from sqlmodel import Field, SQLModel


# class NotaUsuarioLink(SQLModel, table=True):
#     usuario_id: int | None = Field(default=None, foreign_key="usuario.id", primary_key=True)